<h1>Show</h1>
<%= link_to "Edit", edit_blog_post_path(@blog_posts) if user_signed_in? %>
<%= link_to "Back", edit_blog_post_path(@blog_posts) %>

<h1><%= @blog_post.title %></h1>
<%= @blog_post.body %>

<p>
<strong>Comments:</strong>
 <%= render @blog_post.comments.where(parent_id: nil).includes(:user).order(id: :desc) %>
 <%= render partial: 'comments/form', locals: { blog_post: @blog_post, parent: nil } %>  
</p>

